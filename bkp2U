#!/usr/bin/env bash

if [ ! -z "${DEV_BASE// }" ]
then
  cd $DEV_BASE/go/src/github.com/jddixon/xgo
  
  blListGen \
    -d ../xgo \
    -1L  \
    -u $DVCZ_UDIR \
    -X mdZZZ \
    -X .ssh \
    $@
  
else
  echo "DEV_BASE is not defined"
fi
